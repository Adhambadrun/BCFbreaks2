# BCFbreaks — Vercel import template. PUBLIC values only.
# This repository is PUBLIC: never commit AUTH0_CLIENT_SECRET / AUTH0_SECRET /
# DATABASE_URL here (repo rule: "NEVER commit real credentials to git"; a leaked
# AUTH0_SECRET forges session cookies for any email).
# The complete attachable copy (with a generated AUTH0_SECRET) is the workspace
# file vercel.env in your Arena session — download it and use Vercel "Import .env".
AUTH0_DOMAIN=icfg-5qgdjxyskxeyawhf3smwvjne.us.auth0.com
AUTH0_CLIENT_ID=XJS0prsHuy59Gxp15wG3sByvv8sYnyGv
AUTH0_CLIENT_SECRET=
AUTH0_SECRET=
APP_BASE_URL=https://bcflights.vercel.app
DATABASE_URL=
